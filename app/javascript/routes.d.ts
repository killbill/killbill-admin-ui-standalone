/**
 * File generated by js-routes RubyVariables.GEM_VERSION
 * Based on Rails RubyVariables.RAILS_VERSION routes of RubyVariables.APP_CLASS
 */
declare type Optional<T> = {
    [P in keyof T]?: T[P] | null;
};
declare type BaseRouteParameter = string | boolean | Date | number;
declare type MethodRouteParameter = BaseRouteParameter | (() => BaseRouteParameter);
declare type ModelRouteParameter = {
    id: MethodRouteParameter;
} | {
    to_param: MethodRouteParameter;
} | {
    toParam: MethodRouteParameter;
};
declare type RequiredRouteParameter = BaseRouteParameter | ModelRouteParameter;
declare type OptionalRouteParameter = undefined | null | RequiredRouteParameter;
declare type QueryRouteParameter = OptionalRouteParameter | QueryRouteParameter[] | {
    [k: string]: QueryRouteParameter;
};
declare type RouteParameters = Record<string, QueryRouteParameter>;
declare type Serializable = Record<string, unknown>;
declare type Serializer = (value: Serializable) => string;
declare type RouteHelperExtras = {
    requiredParams(): string[];
    toString(): string;
};
declare type RequiredParameters<T extends number> = T extends 1 ? [RequiredRouteParameter] : T extends 2 ? [RequiredRouteParameter, RequiredRouteParameter] : T extends 3 ? [RequiredRouteParameter, RequiredRouteParameter, RequiredRouteParameter] : T extends 4 ? [
    RequiredRouteParameter,
    RequiredRouteParameter,
    RequiredRouteParameter,
    RequiredRouteParameter
] : RequiredRouteParameter[];
declare type RouteHelperOptions<T extends string> = RouteOptions & Optional<Record<T, OptionalRouteParameter>>;
declare type RouteHelper<T extends number = number, U extends string = string> = ((...args: [...RequiredParameters<T>, RouteHelperOptions<U>]) => string) & RouteHelperExtras;
declare type RouteHelpers = Record<string, RouteHelper>;
declare type Configuration = {
    prefix: string;
    default_url_options: RouteParameters;
    special_options_key: string;
    serializer: Serializer;
};
interface RouterExposedMethods {
    config(): Configuration;
    configure(arg: Partial<Configuration>): Configuration;
    serialize: Serializer;
}
declare type KeywordUrlOptions = Optional<{
    host: string;
    protocol: string;
    subdomain: string;
    port: string | number;
    anchor: string;
    trailing_slash: boolean;
    params: RouteParameters;
}>;
declare type RouteOptions = KeywordUrlOptions & RouteParameters;
declare type PartsTable = Record<string, {
    r?: boolean;
    d?: OptionalRouteParameter;
}>;
declare type ModuleType = "CJS" | "AMD" | "UMD" | "ESM" | "DTS" | "NIL";
declare const RubyVariables: {
    PREFIX: string;
    DEPRECATED_GLOBBING_BEHAVIOR: boolean;
    SPECIAL_OPTIONS_KEY: string;
    DEFAULT_URL_OPTIONS: RouteParameters;
    SERIALIZER: Serializer;
    ROUTES_OBJECT: RouteHelpers;
    MODULE_TYPE: ModuleType;
    WRAPPER: <T>(callback: T) => T;
};
declare const define: undefined | (((arg: unknown[], callback: () => unknown) => void) & {
    amd?: unknown;
});
declare const module: {
    exports: any;
} | undefined;
export const configure: RouterExposedMethods['configure'];

export const config: RouterExposedMethods['config'];

export const serialize: RouterExposedMethods['serialize'];

/**
 * Generates rails route to
 * /main/available_engines(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const available_engines_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /main/available_engines(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const available_engines_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /avatax
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const avatax_engine_url: ((
  options?: {} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /avatax
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const avatax_engine_path: ((
  options?: {} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /avatax/
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const avatax_engine_root_url: ((
  options?: {} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /avatax/
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const avatax_engine_root_path: ((
  options?: {} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /avatax/configuration(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const avatax_engine_configuration_index_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /avatax/configuration(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const avatax_engine_configuration_index_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /avatax/configuration/tax_code(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const avatax_engine_set_tax_code_configuration_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /avatax/configuration/tax_code(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const avatax_engine_set_tax_code_configuration_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /avatax/configuration/tax_code(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const avatax_engine_do_set_tax_code_configuration_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /avatax/configuration/tax_code(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const avatax_engine_do_set_tax_code_configuration_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /avatax/configuration/tax_code(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const avatax_engine_remove_tax_code_configuration_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /avatax/configuration/tax_code(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const avatax_engine_remove_tax_code_configuration_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /avatax/configuration/exemption(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const avatax_engine_set_exemption_configuration_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /avatax/configuration/exemption(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const avatax_engine_set_exemption_configuration_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /avatax/configuration/exemption(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const avatax_engine_do_set_exemption_configuration_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /avatax/configuration/exemption(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const avatax_engine_do_set_exemption_configuration_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /avatax/configuration/exemption(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const avatax_engine_remove_exemption_configuration_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /avatax/configuration/exemption(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const avatax_engine_remove_exemption_configuration_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /avatax/configuration/plugin(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const avatax_engine_plugin_configuration_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /avatax/configuration/plugin(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const avatax_engine_plugin_configuration_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /avatax/configuration/plugin(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const avatax_engine_update_plugin_configuration_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /avatax/configuration/plugin(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const avatax_engine_update_plugin_configuration_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /deposit
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const deposit_engine_url: ((
  options?: {} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /deposit
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const deposit_engine_path: ((
  options?: {} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /deposit/
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const deposit_engine_root_url: ((
  options?: {} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /deposit/
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const deposit_engine_root_path: ((
  options?: {} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /deposit/collection(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const deposit_engine_collection_index_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /deposit/collection(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const deposit_engine_collection_index_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /deposit/collection/record(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const deposit_engine_do_record_payments_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /deposit/collection/record(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const deposit_engine_do_record_payments_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /deposit/collection/account_invoices(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const deposit_engine_account_invoices_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /deposit/collection/account_invoices(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const deposit_engine_account_invoices_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /rails/conductor/action_mailbox/inbound_emails/:id/edit(.:format)
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const edit_rails_conductor_inbound_email_url: ((
  id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /rails/conductor/action_mailbox/inbound_emails/:id/edit(.:format)
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const edit_rails_conductor_inbound_email_path: ((
  id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /analytics
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kanaui_engine_url: ((
  options?: {} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /analytics
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kanaui_engine_path: ((
  options?: {} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /analytics/
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kanaui_engine_root_url: ((
  options?: {} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /analytics/
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kanaui_engine_root_path: ((
  options?: {} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /analytics/dashboard(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kanaui_engine_dashboard_index_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /analytics/dashboard(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kanaui_engine_dashboard_index_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /analytics/dashboard/available_reports(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kanaui_engine_available_reports_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /analytics/dashboard/available_reports(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kanaui_engine_available_reports_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /analytics/dashboard/reports(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kanaui_engine_reports_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /analytics/dashboard/reports(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kanaui_engine_reports_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /analytics/reports/new(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kanaui_engine_new_report_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /analytics/reports/new(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kanaui_engine_new_report_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /analytics/reports/:id/edit(.:format)
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kanaui_engine_edit_report_url: ((
  id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /analytics/reports/:id/edit(.:format)
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kanaui_engine_edit_report_path: ((
  id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /analytics/reports/:id(.:format)
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kanaui_engine_report_url: ((
  id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /analytics/reports/:id(.:format)
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kanaui_engine_report_path: ((
  id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /analytics/reports/refresh/:id(.:format)
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kanaui_engine_refresh_report_url: ((
  id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /analytics/reports/refresh/:id(.:format)
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kanaui_engine_refresh_report_path: ((
  id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /analytics/settings(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kanaui_engine_settings_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /analytics/settings(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kanaui_engine_settings_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_url: ((
  options?: {} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_path: ((
  options?: {} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //users/sign_in(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_new_user_session_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //users/sign_in(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_new_user_session_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //users/sign_in(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_user_session_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //users/sign_in(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_user_session_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //users/sign_out(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_destroy_user_session_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //users/sign_out(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_destroy_user_session_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //users/cancel(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_cancel_user_registration_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //users/cancel(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_cancel_user_registration_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //users/sign_up(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_new_user_registration_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //users/sign_up(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_new_user_registration_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //users/edit(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_edit_user_registration_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //users/edit(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_edit_user_registration_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //users(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_user_registration_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //users(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_user_registration_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_kaui_url: ((
  options?: {} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_kaui_path: ((
  options?: {} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/pagination(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_accounts_pagination_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/pagination(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_accounts_pagination_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/validate_external_key(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_accounts_validate_external_key_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/validate_external_key(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_accounts_validate_external_key_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/email_notifications(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_email_notifications_configuration_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/email_notifications(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_email_notifications_configuration_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/email_notifications/events_to_consider(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_email_notification_events_to_consider_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/email_notifications/events_to_consider(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_email_notification_events_to_consider_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/next_invoice_date(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_next_invoice_date_url: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/next_invoice_date(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_next_invoice_date_path: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/trigger_invoice(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_trigger_invoice_url: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/trigger_invoice(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_trigger_invoice_path: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/link_to_parent(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_link_to_parent_url: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/link_to_parent(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_link_to_parent_path: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/unlink_to_parent(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_unlink_to_parent_url: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/unlink_to_parent(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_unlink_to_parent_path: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/account_tags(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_account_tags_url: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/account_tags(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_account_tags_path: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/account_tags/edit(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_edit_account_tags_url: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/account_tags/edit(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_edit_account_tags_path: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/account_tags/edit(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_update_account_tags_url: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/account_tags/edit(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_update_account_tags_path: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/bundle_tags/edit(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_edit_bundle_tags_url: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/bundle_tags/edit(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_edit_bundle_tags_path: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/bundle_tags/edit(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_update_bundle_tags_url: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/bundle_tags/edit(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_update_bundle_tags_path: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/invoice_tags/edit(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_edit_invoice_tags_url: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/invoice_tags/edit(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_edit_invoice_tags_path: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/invoice_tags/edit(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_update_invoice_tags_url: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/invoice_tags/edit(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_update_invoice_tags_path: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/timeline(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_account_timeline_url: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/timeline(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_account_timeline_path: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/custom_fields(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_account_custom_fields_url: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/custom_fields(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_account_custom_fields_path: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/account_children(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_account_children_url: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/account_children(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_account_children_path: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/account_children/pagination(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_account_children_pagination_url: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/account_children/pagination(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_account_children_pagination_path: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/audit_logs/history(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_audit_logs_history_url: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/audit_logs/history(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_audit_logs_history_path: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/set_default_payment_method(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_set_default_payment_method_account_url: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/set_default_payment_method(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_set_default_payment_method_account_path: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/delete_payment_method(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_delete_payment_method_account_url: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/delete_payment_method(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_delete_payment_method_account_path: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/toggle_email_notifications(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_toggle_email_notifications_account_url: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/toggle_email_notifications(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_toggle_email_notifications_account_path: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/pay_all_invoices(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_pay_all_invoices_account_url: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/pay_all_invoices(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_pay_all_invoices_account_path: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/emails(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_account_account_emails_url: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/emails(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_account_account_emails_path: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/emails/new(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_new_account_account_email_url: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/emails/new(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_new_account_account_email_path: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/emails/:id(.:format)
 * @param {any} account_id
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_account_account_email_url: ((
  account_id: RequiredRouteParameter,
  id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/emails/:id(.:format)
 * @param {any} account_id
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_account_account_email_path: ((
  account_id: RequiredRouteParameter,
  id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/bundles(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_account_bundles_url: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/bundles(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_account_bundles_path: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/charges(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_account_charges_url: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/charges(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_account_charges_path: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/charges/new(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_new_account_charge_url: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/charges/new(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_new_account_charge_path: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/chargebacks(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_account_chargebacks_url: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/chargebacks(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_account_chargebacks_path: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/chargebacks/new(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_new_account_chargeback_url: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/chargebacks/new(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_new_account_chargeback_path: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/credits(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_account_credits_url: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/credits(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_account_credits_path: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/credits/new(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_new_account_credit_url: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/credits/new(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_new_account_credit_path: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/invoices(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_account_invoices_url: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/invoices(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_account_invoices_path: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/invoices/:id(.:format)
 * @param {any} account_id
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_account_invoice_url: ((
  account_id: RequiredRouteParameter,
  id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/invoices/:id(.:format)
 * @param {any} account_id
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_account_invoice_path: ((
  account_id: RequiredRouteParameter,
  id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/invoice_items/:id/edit(.:format)
 * @param {any} account_id
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_edit_account_invoice_item_url: ((
  account_id: RequiredRouteParameter,
  id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/invoice_items/:id/edit(.:format)
 * @param {any} account_id
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_edit_account_invoice_item_path: ((
  account_id: RequiredRouteParameter,
  id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/payments(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_account_payments_url: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/payments(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_account_payments_path: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/payments/new(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_new_account_payment_url: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/payments/new(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_new_account_payment_path: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/payments/:id(.:format)
 * @param {any} account_id
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_account_payment_url: ((
  account_id: RequiredRouteParameter,
  id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/payments/:id(.:format)
 * @param {any} account_id
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_account_payment_path: ((
  account_id: RequiredRouteParameter,
  id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/refunds(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_account_refunds_url: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/refunds(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_account_refunds_path: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/refunds/new(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_new_account_refund_url: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/refunds/new(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_new_account_refund_path: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/transactions(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_account_transactions_url: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/transactions(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_account_transactions_path: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/transactions/new(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_new_account_transaction_url: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/transactions/new(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_new_account_transaction_path: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/queues(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_account_queues_url: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/queues(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_account_queues_path: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/audit_logs(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_account_audit_logs_url: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/audit_logs(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_account_audit_logs_path: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_accounts_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_accounts_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/new(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_new_account_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/new(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_new_account_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/edit(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_edit_account_url: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id/edit(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_edit_account_path: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_account_url: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //accounts/:account_id(.:format)
 * @param {any} account_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_account_path: ((
  account_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //payment_methods/validate_external_key(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_payment_methods_validate_external_key_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //payment_methods/validate_external_key(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_payment_methods_validate_external_key_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //payment_methods/refresh(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_refresh_payment_methods_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //payment_methods/refresh(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_refresh_payment_methods_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //payment_methods(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_payment_methods_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //payment_methods(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_payment_methods_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //payment_methods/new(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_new_payment_method_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //payment_methods/new(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_new_payment_method_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //payment_methods/:id(.:format)
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_payment_method_url: ((
  id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //payment_methods/:id(.:format)
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_payment_method_path: ((
  id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //invoices/pagination(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_invoices_pagination_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //invoices/pagination(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_invoices_pagination_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //invoices/:id/show_html(.:format)
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_show_html_invoice_url: ((
  id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //invoices/:id/show_html(.:format)
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_show_html_invoice_path: ((
  id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //invoices/:id(.:format)
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_invoice_url: ((
  id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //invoices/:id(.:format)
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_invoice_path: ((
  id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //invoices/commit(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_commit_invoice_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //invoices/commit(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_commit_invoice_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //invoices/void(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_void_invoice_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //invoices/void(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_void_invoice_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //invoices(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_invoices_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //invoices(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_invoices_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //invoice_items/:id/tags(.:format)
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_update_invoice_items_tags_url: ((
  id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //invoice_items/:id/tags(.:format)
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_update_invoice_items_tags_path: ((
  id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //invoice_items/:id(.:format)
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_invoice_item_url: ((
  id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //invoice_items/:id(.:format)
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_invoice_item_path: ((
  id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //payments/pagination(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_payments_pagination_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //payments/pagination(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_payments_pagination_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //payments/:id(.:format)
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_payment_url: ((
  id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //payments/:id(.:format)
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_payment_path: ((
  id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //payments/:id/cancel_scheduled_payment(.:format)
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_payment_cancel_scheduled_payment_url: ((
  id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //payments/:id/cancel_scheduled_payment(.:format)
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_payment_cancel_scheduled_payment_path: ((
  id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //payments(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_payments_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //payments(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_payments_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //transactions/:id(.:format)
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_transaction_url: ((
  id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //transactions/:id(.:format)
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_transaction_path: ((
  id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //transactions/fix_transaction_state(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_fix_transaction_state_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //transactions/fix_transaction_state(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_fix_transaction_state_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //bundles/:id/do_pause_resume(.:format)
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_do_pause_resume_bundle_url: ((
  id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //bundles/:id/do_pause_resume(.:format)
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_do_pause_resume_bundle_path: ((
  id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //bundles/:id/pause_resume(.:format)
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_pause_resume_bundle_url: ((
  id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //bundles/:id/pause_resume(.:format)
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_pause_resume_bundle_path: ((
  id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //bundles/:id/do_transfer(.:format)
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_do_transfer_bundle_url: ((
  id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //bundles/:id/do_transfer(.:format)
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_do_transfer_bundle_path: ((
  id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //bundles/:id/transfer(.:format)
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_transfer_bundle_url: ((
  id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //bundles/:id/transfer(.:format)
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_transfer_bundle_path: ((
  id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //bundles/:id(.:format)
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_bundle_url: ((
  id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //bundles/:id(.:format)
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_bundle_path: ((
  id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //subscriptions/:id/tags(.:format)
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_update_subscriptions_tags_url: ((
  id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //subscriptions/:id/tags(.:format)
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_update_subscriptions_tags_path: ((
  id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //subscriptions/:id/edit_bcd(.:format)
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_edit_bcd_url: ((
  id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //subscriptions/:id/edit_bcd(.:format)
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_edit_bcd_path: ((
  id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //subscriptions/:id/update_bcd(.:format)
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_update_bcd_url: ((
  id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //subscriptions/:id/update_bcd(.:format)
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_update_bcd_path: ((
  id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //subscriptions/:id/reinstate(.:format)
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_reinstate_url: ((
  id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //subscriptions/:id/reinstate(.:format)
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_reinstate_path: ((
  id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //subscriptions/validate_external_key(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_subscriptions_validate_external_key_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //subscriptions/validate_external_key(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_subscriptions_validate_external_key_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //subscriptions/validate_bundle_external_key(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_subscriptions_validate_bundle_external_key_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //subscriptions/validate_bundle_external_key(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_subscriptions_validate_bundle_external_key_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //subscriptions(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_subscriptions_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //subscriptions(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_subscriptions_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //subscriptions/new(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_new_subscription_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //subscriptions/new(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_new_subscription_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //subscriptions/:id/edit(.:format)
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_edit_subscription_url: ((
  id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //subscriptions/:id/edit(.:format)
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_edit_subscription_path: ((
  id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //subscriptions/:id(.:format)
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_subscription_url: ((
  id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //subscriptions/:id(.:format)
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_subscription_path: ((
  id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //tags/pagination(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_tags_pagination_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //tags/pagination(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_tags_pagination_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //tags(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_tags_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //tags(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_tags_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //tag_definitions(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_tag_definitions_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //tag_definitions(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_tag_definitions_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //tag_definitions/new(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_new_tag_definition_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //tag_definitions/new(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_new_tag_definition_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //tag_definitions/:id(.:format)
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_tag_definition_url: ((
  id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //tag_definitions/:id(.:format)
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_tag_definition_path: ((
  id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //custom_fields/pagination(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_custom_fields_pagination_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //custom_fields/pagination(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_custom_fields_pagination_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //custom_fields/check_object_exist(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_custom_fields_check_object_exist_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //custom_fields/check_object_exist(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_custom_fields_check_object_exist_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //custom_fields(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_custom_fields_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //custom_fields(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_custom_fields_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //custom_fields/new(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_new_custom_field_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //custom_fields/new(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_new_custom_field_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //tenants(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_tenants_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //tenants(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_tenants_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //tenants/select_tenant(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_select_tenant_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //tenants/select_tenant(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_select_tenant_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //login_proxy/check_login(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_check_login_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //login_proxy/check_login(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_check_login_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //home(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_home_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //home(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_home_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //home/search(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_search_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //home/search(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_search_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //queues(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_queues_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //queues(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_queues_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //admin(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_admin_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //admin(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_admin_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //admin/clock(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_admin_set_clock_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //admin/clock(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_admin_set_clock_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //admin_tenants/:id/new_catalog(.:format)
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_admin_tenant_new_catalog_url: ((
  id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //admin_tenants/:id/new_catalog(.:format)
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_admin_tenant_new_catalog_path: ((
  id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //admin_tenants/:id/delete_catalog(.:format)
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_admin_tenant_delete_catalog_url: ((
  id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //admin_tenants/:id/delete_catalog(.:format)
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_admin_tenant_delete_catalog_path: ((
  id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //admin_tenants/:id/new_plan_currency(.:format)
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_admin_tenant_new_plan_currency_url: ((
  id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //admin_tenants/:id/new_plan_currency(.:format)
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_admin_tenant_new_plan_currency_path: ((
  id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //admin_tenants/:id/new_overdue_config(.:format)
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_admin_tenant_new_overdue_config_url: ((
  id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //admin_tenants/:id/new_overdue_config(.:format)
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_admin_tenant_new_overdue_config_path: ((
  id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //admin_tenants/upload_catalog(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_admin_tenant_upload_catalog_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //admin_tenants/upload_catalog(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_admin_tenant_upload_catalog_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //admin_tenants/display_catalog_xml(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_admin_tenant_display_catalog_xml_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //admin_tenants/display_catalog_xml(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_admin_tenant_display_catalog_xml_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //admin_tenants/display_overdue_xml(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_admin_tenant_display_overdue_xml_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //admin_tenants/display_overdue_xml(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_admin_tenant_display_overdue_xml_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //admin_tenants/create_simple_plan(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_admin_tenant_create_simple_plan_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //admin_tenants/create_simple_plan(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_admin_tenant_create_simple_plan_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //admin_tenants/modify_overdue_config(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_admin_tenant_modify_overdue_config_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //admin_tenants/modify_overdue_config(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_admin_tenant_modify_overdue_config_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //admin_tenants/upload_overdue_config(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_admin_tenant_upload_overdue_config_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //admin_tenants/upload_overdue_config(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_admin_tenant_upload_overdue_config_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //admin_tenants/upload_invoice_template(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_admin_tenant_upload_invoice_template_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //admin_tenants/upload_invoice_template(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_admin_tenant_upload_invoice_template_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //admin_tenants/upload_invoice_translation(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_admin_tenant_upload_invoice_translation_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //admin_tenants/upload_invoice_translation(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_admin_tenant_upload_invoice_translation_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //admin_tenants/upload_catalog_translation(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_admin_tenant_upload_catalog_translation_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //admin_tenants/upload_catalog_translation(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_admin_tenant_upload_catalog_translation_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //admin_tenants/upload_plugin_config(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_admin_tenant_upload_plugin_config_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //admin_tenants/upload_plugin_config(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_admin_tenant_upload_plugin_config_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //admin_tenants/remove_allowed_user(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_remove_allowed_user_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //admin_tenants/remove_allowed_user(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_remove_allowed_user_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //admin_tenants/add_allowed_user(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_add_allowed_user_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //admin_tenants/add_allowed_user(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_add_allowed_user_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //admin_tenants/allowed_users(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_admin_tenant_allowed_users_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //admin_tenants/allowed_users(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_admin_tenant_allowed_users_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //admin_tenants/catalog_by_effective_date(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_catalog_by_effective_date_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //admin_tenants/catalog_by_effective_date(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_catalog_by_effective_date_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //admin_tenants/switch(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_switch_tenant_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //admin_tenants/switch(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_switch_tenant_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //admin_tenants/:id/download_catalog(.:format)
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_download_catalog_xml_url: ((
  id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //admin_tenants/:id/download_catalog(.:format)
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_download_catalog_xml_path: ((
  id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //admin_tenants(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_admin_tenants_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //admin_tenants(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_admin_tenants_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //admin_tenants/new(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_new_admin_tenant_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //admin_tenants/new(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_new_admin_tenant_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //admin_tenants/:id(.:format)
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_admin_tenant_url: ((
  id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //admin_tenants/:id(.:format)
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_admin_tenant_path: ((
  id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //admin_allowed_users(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_admin_allowed_users_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //admin_allowed_users(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_admin_allowed_users_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //admin_allowed_users/new(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_new_admin_allowed_user_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //admin_allowed_users/new(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_new_admin_allowed_user_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //admin_allowed_users/:id/edit(.:format)
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_edit_admin_allowed_user_url: ((
  id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //admin_allowed_users/:id/edit(.:format)
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_edit_admin_allowed_user_path: ((
  id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //admin_allowed_users/:id(.:format)
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_admin_allowed_user_url: ((
  id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //admin_allowed_users/:id(.:format)
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_admin_allowed_user_path: ((
  id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //admin_allowed_users/add_tenant(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_add_tenant_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //admin_allowed_users/add_tenant(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_add_tenant_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //role_definitions(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_role_definitions_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //role_definitions(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_role_definitions_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //role_definitions/new(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_new_role_definition_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * //role_definitions/new(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kaui_engine_new_role_definition_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /kenui
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kenui_engine_url: ((
  options?: {} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /kenui
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kenui_engine_path: ((
  options?: {} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /kenui/
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kenui_engine_root_url: ((
  options?: {} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /kenui/
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kenui_engine_root_path: ((
  options?: {} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /kenui/email_notifications(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kenui_engine_email_notifications_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /kenui/email_notifications(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kenui_engine_email_notifications_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /kenui/email_notifications/pagination(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kenui_engine_email_notification_pagination_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /kenui/email_notifications/pagination(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kenui_engine_email_notification_pagination_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /kenui/email_notifications/configuration(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kenui_engine_email_notifications_get_configuration_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /kenui/email_notifications/configuration(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kenui_engine_email_notifications_get_configuration_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /kenui/email_notifications/configuration(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kenui_engine_email_notifications_configuration_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /kenui/email_notifications/configuration(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kenui_engine_email_notifications_configuration_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /kenui/email_notifications/events_to_consider(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kenui_engine_email_notification_events_to_consider_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /kenui/email_notifications/events_to_consider(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kenui_engine_email_notification_events_to_consider_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /kpm
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kpm_engine_url: ((
  options?: {} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /kpm
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kpm_engine_path: ((
  options?: {} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /kpm/
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kpm_engine_root_url: ((
  options?: {} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /kpm/
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kpm_engine_root_path: ((
  options?: {} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /kpm/nodes_info(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kpm_engine_nodes_info_index_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /kpm/nodes_info(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kpm_engine_nodes_info_index_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /kpm/plugins(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kpm_engine_plugins_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /kpm/plugins(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kpm_engine_plugins_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /kpm/nodes_info/refresh(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kpm_engine_nodes_info_refresh_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /kpm/nodes_info/refresh(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kpm_engine_nodes_info_refresh_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /kpm/nodes_info/plugin/install(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kpm_engine_plugin_install_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /kpm/nodes_info/plugin/install(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kpm_engine_plugin_install_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /kpm/nodes_info/plugin/install_from_fs(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kpm_engine_plugin_install_from_fs_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /kpm/nodes_info/plugin/install_from_fs(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kpm_engine_plugin_install_from_fs_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /kpm/nodes_info/plugin/uninstall(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kpm_engine_plugin_uninstall_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /kpm/nodes_info/plugin/uninstall(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kpm_engine_plugin_uninstall_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /kpm/nodes_info/plugin/start(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kpm_engine_plugin_start_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /kpm/nodes_info/plugin/start(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kpm_engine_plugin_start_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /kpm/nodes_info/plugin/stop(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kpm_engine_plugin_stop_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /kpm/nodes_info/plugin/stop(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kpm_engine_plugin_stop_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /kpm/nodes_info/plugin/restart(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kpm_engine_plugin_restart_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /kpm/nodes_info/plugin/restart(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const kpm_engine_plugin_restart_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /rails/conductor/action_mailbox/inbound_emails/new(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const new_rails_conductor_inbound_email_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /rails/conductor/action_mailbox/inbound_emails/new(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const new_rails_conductor_inbound_email_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /payment_test
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const payment_test_engine_url: ((
  options?: {} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /payment_test
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const payment_test_engine_path: ((
  options?: {} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /payment_test/
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const payment_test_engine_root_url: ((
  options?: {} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /payment_test/
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const payment_test_engine_root_path: ((
  options?: {} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /payment_test/payments(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const payment_test_engine_payments_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /payment_test/payments(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const payment_test_engine_payments_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /payment_test/payments/set_failed_state(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const payment_test_engine_set_failed_state_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /payment_test/payments/set_failed_state(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const payment_test_engine_set_failed_state_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /payment_test/payments/reset(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const payment_test_engine_reset_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /payment_test/payments/reset(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const payment_test_engine_reset_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /rails/active_storage/representations/:signed_blob_id/:variation_key/*filename(.:format)
 * @param {any} signed_blob_id
 * @param {any} variation_key
 * @param {any} filename
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const rails_blob_representation_url: ((
  signed_blob_id: RequiredRouteParameter,
  variation_key: RequiredRouteParameter,
  filename: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /rails/active_storage/representations/:signed_blob_id/:variation_key/*filename(.:format)
 * @param {any} signed_blob_id
 * @param {any} variation_key
 * @param {any} filename
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const rails_blob_representation_path: ((
  signed_blob_id: RequiredRouteParameter,
  variation_key: RequiredRouteParameter,
  filename: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /rails/conductor/action_mailbox/inbound_emails/:id(.:format)
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const rails_conductor_inbound_email_url: ((
  id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /rails/conductor/action_mailbox/inbound_emails/:id(.:format)
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const rails_conductor_inbound_email_path: ((
  id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /rails/conductor/action_mailbox/:inbound_email_id/reroute(.:format)
 * @param {any} inbound_email_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const rails_conductor_inbound_email_reroute_url: ((
  inbound_email_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /rails/conductor/action_mailbox/:inbound_email_id/reroute(.:format)
 * @param {any} inbound_email_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const rails_conductor_inbound_email_reroute_path: ((
  inbound_email_id: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /rails/conductor/action_mailbox/inbound_emails(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const rails_conductor_inbound_emails_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /rails/conductor/action_mailbox/inbound_emails(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const rails_conductor_inbound_emails_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /rails/active_storage/direct_uploads(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const rails_direct_uploads_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /rails/active_storage/direct_uploads(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const rails_direct_uploads_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /rails/active_storage/disk/:encoded_key/*filename(.:format)
 * @param {any} encoded_key
 * @param {any} filename
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const rails_disk_service_url: ((
  encoded_key: RequiredRouteParameter,
  filename: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /rails/active_storage/disk/:encoded_key/*filename(.:format)
 * @param {any} encoded_key
 * @param {any} filename
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const rails_disk_service_path: ((
  encoded_key: RequiredRouteParameter,
  filename: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /rails/info(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const rails_info_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /rails/info(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const rails_info_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /rails/info/properties(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const rails_info_properties_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /rails/info/properties(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const rails_info_properties_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /rails/info/routes(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const rails_info_routes_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /rails/info/routes(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const rails_info_routes_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /rails/mailers(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const rails_mailers_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /rails/mailers(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const rails_mailers_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /rails/action_mailbox/mailgun/inbound_emails/mime(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const rails_mailgun_inbound_emails_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /rails/action_mailbox/mailgun/inbound_emails/mime(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const rails_mailgun_inbound_emails_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /rails/action_mailbox/mandrill/inbound_emails(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const rails_mandrill_inbound_emails_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /rails/action_mailbox/mandrill/inbound_emails(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const rails_mandrill_inbound_emails_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /rails/action_mailbox/mandrill/inbound_emails(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const rails_mandrill_inbound_health_check_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /rails/action_mailbox/mandrill/inbound_emails(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const rails_mandrill_inbound_health_check_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /rails/action_mailbox/postmark/inbound_emails(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const rails_postmark_inbound_emails_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /rails/action_mailbox/postmark/inbound_emails(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const rails_postmark_inbound_emails_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /rails/action_mailbox/relay/inbound_emails(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const rails_relay_inbound_emails_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /rails/action_mailbox/relay/inbound_emails(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const rails_relay_inbound_emails_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /rails/action_mailbox/sendgrid/inbound_emails(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const rails_sendgrid_inbound_emails_url: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /rails/action_mailbox/sendgrid/inbound_emails(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const rails_sendgrid_inbound_emails_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /rails/active_storage/blobs/:signed_id/*filename(.:format)
 * @param {any} signed_id
 * @param {any} filename
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const rails_service_blob_url: ((
  signed_id: RequiredRouteParameter,
  filename: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /rails/active_storage/blobs/:signed_id/*filename(.:format)
 * @param {any} signed_id
 * @param {any} filename
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const rails_service_blob_path: ((
  signed_id: RequiredRouteParameter,
  filename: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const root_url: ((
  options?: {} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const root_path: ((
  options?: {} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /rails/active_storage/disk/:encoded_token(.:format)
 * @param {any} encoded_token
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const update_rails_disk_service_url: ((
  encoded_token: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /rails/active_storage/disk/:encoded_token(.:format)
 * @param {any} encoded_token
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const update_rails_disk_service_path: ((
  encoded_token: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

// By some reason this line prevents all types in a file
// from being automatically exported
export {};
